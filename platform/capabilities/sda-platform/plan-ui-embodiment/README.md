# Plan UI embodiment

This pure capability produces immutable `ui-embodiment-plan.v1`. The plan pins
semantic, compiler, presentation IR, capability-vector, target-profile,
provider-catalog, and provider digests; every required feature is mapped to one
provider mechanic. Providers receive the plan rather than rich consumer UI
authority.
