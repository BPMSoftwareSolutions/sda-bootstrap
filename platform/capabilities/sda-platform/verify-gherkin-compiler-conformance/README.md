# Verify Gherkin compiler conformance

This admitted capability owns the full lossless compiler/profile admission
gate. Its scenarios distinguish satisfied partitions, absent proof, and stale
or mismatched evidence. Until every required partition is current and
satisfied, `GHERKIN_SEMANTIC_INGESTION_CONFORMANT` remains unissued. The
content-addressed fixture manifest and durable receipt are under `fixtures/`
and `conformance/`; the target-neutral evaluator is implemented at
`tools/src/gherkin/application/canonical-gherkin-compiler.ts`.
