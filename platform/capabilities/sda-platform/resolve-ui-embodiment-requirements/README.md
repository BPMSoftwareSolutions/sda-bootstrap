# Resolve UI embodiment requirements

This pure capability derives `ui-capability-vector.v1` solely from a canonical
`sda-ui-presentation-ir.v3`. It groups exact normalized mechanics and their
source references, including explicit accessibility and observation proof
requirements. It does not load or select providers.

The reference implementation is
`tools/src/ui-presentation/application/ui-embodiment-planner.ts`.
