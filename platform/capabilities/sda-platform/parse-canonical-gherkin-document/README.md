# Parse canonical Gherkin document

This admitted capability owns exact-byte syntax observation through the pinned
official Gherkin grammar binding. Its scenarios cover successful preservation,
advanced constructs, malformed source, invalid source carriers, and repeated
deterministic parsing. It does not admit SDA meaning or project execution.

The target-neutral provider is implemented at
`tools/src/gherkin/application/canonical-gherkin-compiler.ts` and is admitted
only with the capability-owned compiler conformance receipt.
